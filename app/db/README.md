# Database Layer

This folder contains low-level database access helpers.

- Connection setup belongs here.
- Schema bootstrapping helpers for early slices may live here.
- Higher-level persistence rules belong in `app/repositories/`.

Keep business queries out of this folder.
