# Streamlit UI Layer

This folder is reserved for the MVP Streamlit user experience.

- `pages/` should contain candidate and admin page flows.
- UI code should call backend services or APIs instead of embedding business logic.

Keep Streamlit thin so a future UI replacement does not require rewriting the domain layer.
