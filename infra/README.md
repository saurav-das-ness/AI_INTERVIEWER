# Infrastructure Layer

This folder is reserved for deployment and environment assets.

- `aws/` should contain cloud deployment assets.
- `local/` should contain local environment helpers.

Infrastructure definitions should stay separate from application business code.