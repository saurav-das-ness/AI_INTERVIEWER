# Storage Layer

This folder is reserved for local runtime data.

- `uploads/` stores raw source documents and imported files.
- `chroma/` stores local vector data during development.

Do not commit real runtime data from this folder unless it is intentionally curated test data.
