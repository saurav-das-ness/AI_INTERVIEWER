# Test Layer

This folder contains automated validation for the application.

- `unit/` should hold focused business-rule tests.
- `integration/` should hold slice-level tests across services or persistence.
- `fixtures/` should hold small reusable sample inputs.

Prefer narrow tests that prove requirements and contracts.
